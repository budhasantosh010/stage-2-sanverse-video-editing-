# AI rail contract

Collapsed AI is one 52 px full-height rail with the unambiguous action `Expand AI`. Expanded AI is 320–420 px with `Collapse AI`. The outer `StudioAiPane` is the only toggle authority. ChatComposer remains mounted, and the real browser preserved `keep this exact draft` through collapse, expansion, Effects, Color, Audio, and return to Edit.

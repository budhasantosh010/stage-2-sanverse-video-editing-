# Fixed control-size contract

The Studio toolbar is bounded to 42–48 px and scrolls horizontally rather than growing the editor. Resize handles use an 8 px hit area with visible line/grip states. AI actions keep a minimum 44×44 px target. Reduced-motion rules disable separator and overlay transitions.

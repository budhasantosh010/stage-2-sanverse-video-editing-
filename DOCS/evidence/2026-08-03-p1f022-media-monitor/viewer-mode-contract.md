# Viewer mode contract

- Fit: contain the whole source and preserve letterboxing.
- Fill: cover the monitor and crop only presentation.
- 100%: render source pixels without changing project transform state.

Viewer mode is local presentation state. It never creates an edit or changes export.

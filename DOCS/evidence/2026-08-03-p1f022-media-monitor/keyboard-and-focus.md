# Keyboard and focus

All monitor actions are native buttons/select/ranges with names and visible focus. Point preserves its return-focus contract. Monitor shortcuts ignore inputs, textareas, selects, and contenteditable elements. Fullscreen exits from its button or Escape; unsupported native fullscreen uses a bounded in-app fallback. Reduced-motion behavior remains inherited.

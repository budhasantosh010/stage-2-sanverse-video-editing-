# Transport contract

The custom transport exposes previous frame, play/pause, next frame, frame-accurate timecode, duration, seek, mute, and volume. It controls the existing video/playhead only. Space or K toggles playback and arrow keys step frames when focus is not owned by a text input.

# Media panel architecture

`MediaBin` remains a presentation over the App-owned media view model. It has three vertical zones: fixed compact header, fixed adaptive controls, and one scrollable result region. Search, filters, selection, import callbacks, usage, availability, and existing placement actions retain their prior authority.

No folder, ordering, multiselect, batch, or drag-placement state was introduced.

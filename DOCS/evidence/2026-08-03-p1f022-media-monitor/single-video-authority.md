# Single-video authority

DOM assertions and real-browser checks found exactly one `video` before and after Media resize, viewer-mode changes, Point capture, fullscreen entry/exit, Undo, and Redo. Native controls are absent. The existing `videoRef`, playhead, preview content layer, Canvas, and Point geometry remain shared.

# Media scroll authority

The Studio Media region clips its shell. `.media-bin__results` alone owns vertical Media scrolling. The header, search/import controls, and result count remain fixed. This prevents nested desktop scrolling and protects compact-pane controls.

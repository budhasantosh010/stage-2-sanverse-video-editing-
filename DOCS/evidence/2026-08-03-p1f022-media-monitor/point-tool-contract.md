# Point tool contract

Point is always reachable in the compact monitor toolbar. Pressed state means capture mode; a separate small status mark means a point is selected. Capture pauses playback, rejects letterbox clicks, stores normalized source-space coordinates with exact time, draws inside the same content geometry, then returns to ordinary playback. `P`, keyboard cursor movement, Enter, and Escape preserve existing focus behavior.

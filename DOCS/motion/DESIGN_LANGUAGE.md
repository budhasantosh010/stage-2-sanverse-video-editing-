# Motion Design Language

Every component communicates one primary idea, then secondary information, then restrained tertiary polish.

```text
ENTER -> SETTLE -> HOLD -> EXIT
```

Use composition-relative safe margins. Portrait layouts reflow instead of merely shrinking landscape. Typography favors hierarchy and readability over decorative effects. Style packs provide semantic color, typography, shape, depth and motion tokens so component implementations remain shared.
